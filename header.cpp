#include <iostream>     // cout, cin, cerr
#include <iomanip>      // setw, setprecision, etc.

#include <cmath>        // pow, sqrt, sin, cos, abs, etc.
#include <cstdlib>      // rand, srand, abs, exit
#include <ctime>        // time (for srand)
#include <limits>       // numeric_limits<T>::max(), etc.

#include <vector>
#include <array>
#include <list>
#include <deque>
#include <stack>
#include <queue>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>

#include <algorithm>    // sort, max, min, reverse, find, etc.
#include <numeric>      // accumulate, inner_product, etc.
#include <utility>      // pair, make_pair, swap
#include <functional>   // function, bind, greater, etc.

#include <string>       // std::string
#include <sstream>      // stringstream, istringstream, ostringstream

#include <cctype>       // isdigit, isalpha, toupper, etc.
#include <cassert>      // assert
#include <fstream>      // ifstream, ofstream, file I/O
#include <bitset>       // bitset<N>
#include <tuple>        // tuple, make_tuple
#include <iterator>     // iterators
#include <stdexcept>    // exceptions like invalid_argument

